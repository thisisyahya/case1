<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        /* Import Google Font */
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        /* Reset & Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        /* Body Styles */
        body {
            background: #f5f7fa;
            text-align: center;
            padding-top: 50px;
        }

        /* Container */
        .login-container {
            width: 90%;
            max-width: 400px;
            background: #fff;
            padding: 25px;
            margin: 20px auto;
            border-radius: 8px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
        }

        /* Heading */
        h2 {
            font-size: 24px;
            color: #333;
            margin-bottom: 5px;
        }

        p {
            font-size: 14px;
            color: #777;
        }

        /* Buttons */
        .login-btn {
            display: block;
            width: 90%;
            max-width: 300px;
            padding: 10px;
            margin: 10px auto;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        /* Google Button */
        .google {
            background: #fff;
            border: 1px solid #ccc;
            color: #333;
        }

        /* Facebook Button */
        .facebook {
            background: #1877f2;
            color: #fff;
        }

        /* Button Images */
        .login-btn img {
            width: 20px;
            height: 20px;
        }

        /* OR Line */
        .or {
            margin: 15px 0;
            font-size: 14px;
            color: #777;
        }

        /* Register Link */
        .register {
            font-size: 14px;
            color: #1877f2;
            text-decoration: none;
        }

        /* Responsive */
        @media (max-width: 280px) {
            .login-container {
                width: 95%;
                padding: 15px;
            }
            h2 {
                font-size: 20px;
            }
            .login-btn {
                font-size: 14px;
                padding: 8px;
            }
        }
    </style>
</head>
<body>

    <div class="login-container">
        <h2>Welcome Back!</h2>
        <p>Login to continue</p>

        <a href="/auth/google" class="login-btn google">
            <img src="https://static.vecteezy.com/system/resources/previews/013/948/549/non_2x/google-logo-on-transparent-white-background-free-vector.jpg" alt="Google Logo">
            Login with Google
        </a>

        <a href="/auth/facebook" class="login-btn facebook">
            <img src="https://upload.wikimedia.org/wikipedia/commons/5/51/Facebook_f_logo_%282019%29.svg" alt="Facebook Logo">
            Login with Facebook
        </a>

        <p class="or">— OR —</p>

        <a href="#" class="register">Create an Account</a>
    </div>

</body>
</html>
